@AGENTS.md

"Whenever you create or modify a file, immediately stage the changes (git add), commit them (git commit) with a descriptive message, and push the changes (git push) to the remote repository. ALWAYS push after committing."